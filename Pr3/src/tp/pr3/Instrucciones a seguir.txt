
1º: Crear el navigationModule. Cambiar atributos y funciones que sea necesario cambiar. Crear las nuevas funciones(métodos) en el robotEngine y en 
	el  navigation(excepto el move()).
2º: Crear las excepciones, crear las instrucciones, sin implementar(pueden hacerse los getHelp).
3º: Hacer de cero el Interpreter a la vez que se hace el parse de las instrucciones(van de la mano). Pista: Array con las
	instrucciones que hay que se va recorriendo.
4º: Implementar el configureContext y el execute de las instruccione(empezando por las intrucciones más sencillas) y a la
	vez hacer el Robot Engine de cero. Pista1: Los execute de cada instrucción se parecen a los que hay en el robot engine.
	Pista2: El configure context debe proveer a la instrucción de los elementos necesarios para que se ejecute(Pista en el 
	execute, por lo que hacer antes el execute y completar los parse(Guardad id de objetos)). 
5º: Hacer el move y los out de las excepciones.
6º: Cargador de mapa.
	